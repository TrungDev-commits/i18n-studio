{
  "name": "i18n-studio",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "desktop"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run build && node server/server.js",
    "desktop": "npm run build && npm start --workspace desktop",
    "desktop:dev": "concurrently -n vite,electron -c green,blue \"npm run dev --workspace client\" \"npm run dev --workspace desktop\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}